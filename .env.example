ANTHROPIC_API_KEY=
# Optional:
OPENAI_API_KEY=

# Comma-separated browser origins allowed to call this API (merged with built-in list). Use when
# the frontend hostname is not already allowed — blocked origins are logged as [CORS] Blocked Origin.
# CORS_ALLOWED_ORIGINS=https://my-preview.vercel.app,https://custom.example.com

